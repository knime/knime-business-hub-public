---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: executorgateways.hub.knime.com
spec:
  group: hub.knime.com
  names:
    categories:
    - knime
    kind: ExecutorGateway
    listKind: ExecutorGatewayList
    plural: executorgateways
    shortNames:
    - egw
    singular: executorgateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Endpoint of the ExecutorGateway
      jsonPath: .status.endpoint
      name: Endpoint
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExecutorGateway is the Schema for the ExecutorGateways API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExecutorGatewaySpec defines the desired state of ExecutorGateway.
            properties:
              agent:
                description: Reference to the remote agent this ExecutorGateway belongs
                  to
                properties:
                  id:
                    description: ID of the Agent.
                    type: string
                  version:
                    description: Version of the Agent (e.g., 1.3.2). Used by the controller
                      to pick a config.
                    minLength: 1
                    type: string
                required:
                - id
                - version
                type: object
              executor:
                description: Reference to the remote executor this ExecutorGateway
                  belongs to
                properties:
                  executionContextId:
                    description: ExecutionContextID is the ID of the ExecutionContext
                      this Executor belongs to.
                    type: string
                  id:
                    description: ID of the Executor.
                    type: string
                  version:
                    description: Version of the Executor (e.g., 5.6.1). Just for information.
                    type: string
                required:
                - id
                - version
                type: object
            required:
            - agent
            - executor
            type: object
          status:
            description: ExecutionContextStatus defines the observed state of ExecutionContext.
            properties:
              endpoint:
                description: Endpoint is the address (URL) of the tunnel end point
                  of the ExecutorGateway.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
